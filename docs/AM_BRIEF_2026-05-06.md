# Morning Brief — 2026-05-06

> **TL;DR:** YTD revenue + labor + COGS + events all reconciled and live. The agentic reporting layer (Phase A.2) shipped in 8 PRs (#95–#103) overnight. Tomorrow's daily LLM briefing is auto-scheduled for 13:30 UTC. Two Gmail secrets needed for inbox delivery (otherwise digest renders inside dashboard). Slack still pending Method admin approval — anomaly + reconciler alerts will land via email/dashboard until then.

## YTD reconciliation (Jan 1 → May 5)

| | $ | % of net sales |
|---|---|---|
| **Net Sales** | **$9,492,442** | — |
| Labor (hourly) | $1,929,371 | 20.3% |
| Food COGS (cogs_only) | $2,308,741 | 24.3% |
| **Prime cost** | **$4,238,112** | **44.6%** |
| Events booked (CLOSED+DEFINITE) | $1,473,086 | — |
| Events grand total | $1,897,522 | — |
| Events count | 431 | — |

**44.6% prime cost** is well below the 60% F&B industry target. Portfolio is operationally healthy.

### Per-outlet (top 5 by revenue)

| outlet | YTD net sales | labor% | cogs% | events |
|---|---|---|---|---|
| LSBR | $1,997,443 | 22.5% | 28.6% | 37 |
| Lowland | $1,725,482 | 16.7% | 23.0% | 51 |
| Hiroki Det | $1,348,579 | 19.6% | 25.7% | 13 |
| Mulherins | $1,227,127 | 20.3% | 22.2% | 27 |
| Rosemary & Rose | $879,131 | 14.4% | 20.8% | 30 |

Anthology event venue: 161 events YTD ($592K booked / $748K grand_total) — heaviest event volume in the portfolio.

## What shipped overnight (8 PRs)

| PR | What it does |
|---|---|
| #95 | Net Sales sourced from selections, not check.amount. **+14% inflation eliminated.** |
| #96 | `cross_source_reconciler` agent — auto-flags drift between sources. |
| #97 | **Critical:** Pydantic datetime regression that had silently failed every nightly Toast Sync since 2026-05-04. |
| #98 | Storage upload auth (Supabase `sb_secret` format). |
| #99–#100 | Net Sales formula iterations. Final = Toast Web UI Sales Summary semantics. |
| **#101** | **Phase A.2 agentic reporting layer.** LLM briefings + Python reconciler + email digest + dashboard integration. |
| #102 | Anomaly detector default flipped from shadow → live (backfill provides the 8-week baseline now). |
| #103 | Tripleseat events added to LLM briefing context. |

## How accurate is the data now

LSBR week ending 5/3 reconciliation:

| | $ | vs your Toast direct $125,673.04 |
|---|---|---|
| **Pre-fix dashboard** | $143,315 | +14.04% |
| Post #95 (selection-based) | $129,670 | +3.18% |
| Post #99 (API-spec formula) | $130,861 | +4.13% |
| **Current — post #100 (UI-aligned)** | **$129,670** | **+3.18%** |

The remaining +3% gap likely traces to Toast's business-day cutoff (4 AM ET) vs our calendar-day cutoff (midnight). Bounded by late-night order volume — not a methodology issue.

## Agentic stack — current state

```
data syncs (Toast/MarginEdge/Tripleseat/Resy nightly cron)
  ↓
Pydantic validation (every row, dropped/warned tracked)
  ↓
cross_source_reconciler (Python: 11 outlets × 3 checks each every nightly)
  ↓
reporting_agent (Anthropic Sonnet — per-outlet headline/wins/concerns/actions/outlook)
  ↓
morning_briefing (Method-branded HTML digest, Gmail SMTP to your inbox)
  ↓
dashboard auto-refreshes (LLM Wins/Concerns at top of Weekly Snapshot)
```

**6 agents running every 5 min in the Edge Function:**
1. drift_detector (Anthropic-classified schema drift)
2. anomaly_detector (per-outlet × DOW ±3σ — **just flipped to LIVE**)
3. retry_repair (auto-retries failed workflows up to 3× per 30min)
4. cross_source_reconciler (Edge Function version — Python twin runs via GH Actions)
5. alert_dispatcher (Slack delivery — currently blocked, falling through to email)
6. banner_writer (per-outlet trust state for dashboard validation panel)

**Daily at 13:30 UTC (08:30 ET EST / 09:30 EDT):**
The new `Agentic Reporting` workflow runs reporting_agent → morning_briefing in sequence after the data-sync nightlies. First scheduled run lands tomorrow morning automatically.

## What needs your hands (5 minutes total)

### 1. Add Gmail SMTP secrets to GitHub (so the daily email lands in your inbox)

```
GitHub → Settings → Secrets and variables → Actions → New repository secret

GMAIL_USER         rr@methodco.com
GMAIL_APP_PASS     <16-char app password from https://myaccount.google.com/apppasswords>
                   (requires 2FA enabled on your Google account; NOT your account password)
```

Optional:
```
BRIEFING_TO        rr@methodco.com  (default if unset)
BRIEFING_CC        moshea@methodco.com,jm@methodco.com  (or whoever)
```

If you don't add these tonight: the digest still commits to `data/_snapshots/_morning_briefing.html` and renders inside the dashboard — just no inbox push.

### 2. Deploy the updated Edge Function (so the 6th agent + anomaly live mode go active)

```bash
cd "/Users/rossrichardson/CLAUDE PROJECTS/method-q1-2026-dashboards"
supabase functions deploy agent-worker
```

This activates:
- `cross_source_reconciler` agent in the Edge Function (in addition to the Python version that already runs via GH Actions)
- `anomaly_detector` live mode (was shadow until 2026-05-18; now defaults to today)
- Improved orchestrator comments

### 3. Slack admin approval (already submitted — no further action from you)

Once approved, paste the bot token into the Supabase Edge Function secret `SLACK_BOT_TOKEN`. Anomaly + reconciliation alerts will start posting to channel `C0B1N51L9TN` automatically.

## Where to look tomorrow

- **Dashboard:** https://rrmethodco.github.io/method-q1-2026-dashboards/
  - Each outlet's Weekly Snapshot now leads with the LLM-generated Headline + Wins/Concerns/Action Items + Outlook (turquoise card at top)
  - Falls back to rule-based snapshot if LLM briefing missing
- **Inbox:** if Gmail secrets added, "Method Co Morning Briefing — 2026-05-06" arrives ~09:30 ET
- **Repo:** `data/_snapshots/_morning_briefing.html` (always there)
- **Audit log:** `audit/agent_decisions.jsonl` in Supabase Storage (every agent decision, append-only)

## What's still queued (next session)

- Slack delivery once admin approves
- Toast Sales Summary auto-pull (eliminates the manual CSV import dependency entirely; closes the ~3% UI-vs-API gap)
- Business-day rollover handling (Toast's 4 AM ET cutoff)
- A.2 expansion: GM-targeted email author (per-outlet operator briefings, not just your portfolio digest)
